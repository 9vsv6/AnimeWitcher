name: DM-15 ack recovery GREEN

on:
  push:
    branches:
      - plan/download-manager-reliability
    paths:
      - .github/workflows/dm14-final-acceptance.yml

permissions:
  contents: write

jobs:
  ack-recovery:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          ref: plan/download-manager-reliability
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Confirm DM-15 ack recovery RED
        run: |
          if flutter test test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart --dart-define=ANIMEWITCHER_FIREBASE_API_KEY=test-api-key; then
            echo "DM-15 ack recovery RED unexpectedly passed"
            exit 1
          fi
          echo "DM-15 ack recovery RED reproduced"

      - name: Apply fail-closed ack recovery patch
        run: python3 tool/dm15_apply_ack_recovery.py

      - name: Verify focused GREEN
        run: |
          dart format lib/core/services/download_continued_processing_service.dart test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart
          flutter test test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart --dart-define=ANIMEWITCHER_FIREBASE_API_KEY=test-api-key
          dart run build_runner build --delete-conflicting-outputs
          flutter analyze --no-fatal-warnings --no-fatal-infos lib/core/services/download_continued_processing_service.dart test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart
          git diff --check

      - name: Commit, rebase and reverify
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/core/services/download_continued_processing_service.dart test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart
          if git diff --cached --quiet; then
            echo "DM-15 ack recovery patch already present"
            exit 0
          fi
          git commit -m "fix(downloads): fail closed on stale iOS queue snapshots (DM-15)"
          git reset --hard HEAD
          git clean -fd
          git pull --rebase origin plan/download-manager-reliability
          flutter pub get
          flutter test test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart --dart-define=ANIMEWITCHER_FIREBASE_API_KEY=test-api-key
          dart run build_runner build --delete-conflicting-outputs
          flutter analyze --no-fatal-warnings --no-fatal-infos lib/core/services/download_continued_processing_service.dart test/core/services/ios_native_queue_snapshot_ack_recovery_test.dart test/core/services/ios_native_queue_snapshot_version_guard_test.dart
          git push origin HEAD:plan/download-manager-reliability
